class mangastructure{
    constructor(nombre,	autor, editorial, num_pag,	precio,	resumen, genero, fecha_lanzamiento)
    {
        this.nombre = nombre,
        this.autor = autor,
        this.editorial = editorial,
        this.num_pag = num_pag,
        this.precio = precio,
        this.resumen = resumen,
        this.genero = genero,
        this.fecha_lanzamiento = fecha_lanzamiento
    }
}